@model IEnumerable<DoAnASP.Areas.Admin.Models.RapModel>

@{
    ViewData["Title"] = "Index";
}

<div class="main-content">
    <div class="section__content section__content--p30">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="table-responsive table-responsive-data2">
                        <h1>Rạp</h1>

                        <p>
                            <a class="au-btn au-btn-icon au-btn--green au-btn--small" asp-action="Create">Create New</a>
                        </p>
                        <table class="table table-data2">
                            <thead>
                                <tr >
                                    <th>
                                        Tên rạp
                                    </th>
                                    <th>
                                        Địa chỉ rạp
                                    </th>
                                    <th>
                                        Quận/Huyện
                                    </th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr class="tr-shadow">
                                        <td>
                                            @Html.DisplayFor(modelItem => item.TenRap)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.DiaChiRap)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.quanHuyen.TenQuanHuyen)
                                        </td>
                                        <td>
                                            <div class="table-data-feature">
                                                <a asp-action="Edit" asp-route-id="@item.IdRap" class="item" data-toggle="tooltip" data-placement="top" title="Edit">
                                                    <i class="zmdi zmdi-edit"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@item.IdRap" class="item" data-toggle="tooltip" data-placement="top" title="Delete">
                                                    <i class="zmdi zmdi-delete"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>